name: Push a NuGet package to Azure Artifacts or GitHub Package Registry

on:
  push:
    branches:
      - main

env:
  AZURE_ARTIFACTS_FEED_URL: https://pkgs.dev.azure.com/itpro-devops/_packaging/ITPRO/nuget/v3/index.json
  BUILD_CONFIGURATION: 'Release'    # set this to the appropriate build configuration
  DOTNET_VERSION: '3.1.x' 

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     # Checkout the repo
  #     - uses: actions/checkout@v2

  #     # Setup .NET Core SDK
  #     - name: Setup .NET Core
  #       uses: actions/setup-dotnet@v1
  #       with:
  #         dotnet-version: ${{ env.DOTNET_VERSION }}

  #     # Run dotnet build and package
  #     - name: dotnet build
  #       run: ./build.sh

  az-artifacts-build-and-deploy:
    # needs: build
    runs-on: ubuntu-latest
    steps:
      # Checkout the repo
      - uses: actions/checkout@v2

      # Setup .NET Core SDK
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          source-url: ${{ env.AZURE_ARTIFACTS_FEED_URL }}
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.AZURE_DEVOPS }} 

      # Run dotnet build and package
      - name: dotnet build and publish
        run: ./build.sh

      # Publish the package to Azure Artifacts
      - name: 'dotnet publish'
        run: dotnet nuget push AzureArtifacts ./nuget/*.nupkg -k ${{ secrets.AZURE_DEVOPS }}